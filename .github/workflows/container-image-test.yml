name: Container image runtime test

on:
  pull_request:
    branches:
      - main
    paths:
      - Dockerfile
      - docker-compose.yml
      - files/**
  push:
    branches:
      - main

jobs:
  test-container:
    name: Build and smoke test container image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Ensure Docker Compose is available
        run: docker compose version

      - name: Start services with docker-compose
        run: |
          docker compose down --remove-orphans || true
          docker compose up --build -d

      - name: Wait for Znuny web interface
        run: |
          for i in $(seq 1 30); do
            status=$(curl -s -o /tmp/znuny-response.html -w '%{http_code}' http://127.0.0.1:18080/znuny/index.pl || true)
            if [ "$status" = "200" ] && grep -qE 'Znuny|znuny' /tmp/znuny-response.html; then
              echo "Znuny web interface is reachable"
              exit 0
            fi
            echo "Waiting for Znuny web interface... status=$status ($i/30)"
            sleep 10
          done
          echo "Last response body:"
          cat /tmp/znuny-response.html
          exit 1

      - name: Verify SAML2 metadata endpoint
        run: |
          for i in $(seq 1 30); do
            status=$(curl -k -s -o /tmp/znuny-saml-metadata.html -w '%{http_code}' http://127.0.0.1:18080/saml/metadata || true)
            if [ "$status" = "200" ] && grep -q "EntityDescriptor" /tmp/znuny-saml-metadata.html; then
              echo "SAML2 metadata endpoint is available"
              exit 0
            fi
            echo "Waiting for SAML2 metadata endpoint... status=$status ($i/30)"
            sleep 10
          done
          echo "Last response body:"
          cat /tmp/znuny-saml-metadata.html
          exit 1

      - name: Verify SAML2 configuration was applied
        run: docker compose exec -T znuny grep -q "SAML2::UserIDAttribute" /opt/znuny/Kernel/Config.pm

      - name: Cleanup services
        if: always()
        run: docker compose down --remove-orphans
